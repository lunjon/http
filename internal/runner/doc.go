/*
Package runner provides the functionality for the runner command.

runner command takes a request file, i.e. a JSON or YAML file,
that contains a collection of requests to runner. This package
handles everything from reading parsing the file to running it.
 */
package runner
